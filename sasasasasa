#include <iostream>

using namespace std;

void png (){
    

const int DAYS = 7;

string day_name;

string days[DAYS] = { "Mon", "Tue", "Wed", "Thr", "Fri", "Sat", "Sun"};
     float *temp = new float[DAYS];

for(int i=0; i<DAYS; i++){
    cout << "������� �������� ����������� �� " << days[i] << ": ";
    cin >> temp[i];
    }
    cout<<endl;
    
    cout << "������� ���� ������: ";
    cin >> day_name;
    cout << endl;
    
for (int i=0; i < DAYS; i++){
    if( days[i]==day_name ){
        
    cout << "����������� � ���� ����: " << temp[i];
        }
}
}

int main(){
	png();
	return 0;
}
